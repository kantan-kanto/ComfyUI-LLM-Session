# ComfyUI-LLM-Session Requirements

# Core dependencies
llama-cpp-python>=0.3.16  # Recent JamePeng builds may be needed for newer Vision models
pillow>=10.0.0
numpy>=1.24.0

# Note: Newer Vision/Qwen/Gemma model families can require a JamePeng llama-cpp-python build.
# See README.md and COMPATIBILITY.md for backend build notes.

# Note: ComfyUI and its dependencies (torch, etc.) are assumed to be installed
